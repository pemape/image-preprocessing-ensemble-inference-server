# Docker Compose project name (controls the stack/network/volume name prefix).
COMPOSE_PROJECT_NAME=fundus-inference

# Host port exposed by the inference server container.
# Override this to avoid conflicts with other services on the host.
INFERENCE_HOST_PORT=5005
